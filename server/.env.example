# -base server 配置示例（复制为 .env 后按需改）
HOST=0.0.0.0
PORT=8000

# 草稿/产物存储根目录（绝对路径）
STORAGE_ROOT=./storage

# 出片默认参数
DEFAULT_WIDTH=1080
DEFAULT_HEIGHT=1920
DEFAULT_FPS=30
DEFAULT_DURATION_S=6.0

# 路径 B（ffmpeg 兜底）开关
ENABLE_FFMPEG_FALLBACK=true
